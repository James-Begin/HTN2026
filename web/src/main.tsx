import React from 'react'
import ReactDOM from 'react-dom/client'
import DarioDemo from './DarioDemo'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode><DarioDemo /></React.StrictMode>,
)
